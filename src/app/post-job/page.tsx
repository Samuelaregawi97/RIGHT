 "use client";
import {useState} from "react"; import {useRouter} from "next/navigation";
export default function PostJob(){const [msg,setMsg]=useState("");const router=useRouter();
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const data={title:f.get("title"),description:f.get("description"),skills:String(f.get("skills")).split(",").map(x=>x.trim()).filter(Boolean),budgetMin:Number(f.get("budgetMin")),budgetMax:Number(f.get("budgetMax")),currency:f.get("currency")};const r=await fetch("/api/jobs",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const j=await r.json();if(!r.ok){setMsg(j.error);return}router.push("/jobs")}
return <main className="container"><h1>Post a job</h1><form className="card" onSubmit={submit}><label className="label">Title<input className="input" name="title" required/></label><label className="label">Description<textarea className="input" name="description" rows={8}/></label><label className="label">Skills (comma separated)<input className="input" name="skills" placeholder="typing, transcription, design"/></label><label className="label">Minimum budget<input className="input" type="number" name="budgetMin"/></label><label className="label">Maximum budget<input className="input" type="number" name="budgetMax"/></label><label className="label">Currency<input className="input" name="currency" defaultValue="USD"/></label><button className="btn">Publish job</button>{msg&&<p className="error">{msg}</p>}</form></main>}